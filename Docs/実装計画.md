# JPScreenShot 実装計画

[要求仕様.md](要求仕様.md) を実装に落とすための計画。ディレクトリ構成・XcodeGen 設定・実装順序を定める。

本計画の技術判断は、机上ではなく**実機の SDK（Xcode 26.5 / Swift 6.3.2 / macOS 26.5）で検証済み**である。検証内容は [1. 事前検証の結果](#1-事前検証の結果) に記す。

## 目次

- [1. 事前検証の結果](#1-事前検証の結果)
  - [1.1 Vision: 新 API を採用する（仕様からの変更）](#11-vision-新-api-を採用する仕様からの変更)
  - [1.2 キャプチャ: フィルタ方式が必須（CAP-04 の要）](#12-キャプチャ-フィルタ方式が必須cap-04-の要)
  - [1.3 権限: 署名 ID に紐づく（開発時の落とし穴）](#13-権限-署名-id-に紐づく開発時の落とし穴)
  - [1.4 XcodeGen: project.yml をビルドまで検証済み](#14-xcodegen-projectyml-をビルドまで検証済み)
- [2. ディレクトリ構成](#2-ディレクトリ構成)
- [3. XcodeGen 設定](#3-xcodegen-設定)
- [4. モジュール設計](#4-モジュール設計)
- [5. 実装順序](#5-実装順序)
- [6. 技術的な要注意点](#6-技術的な要注意点)
- [7. 受け入れ基準との対応](#7-受け入れ基準との対応)
- [8. 仕様に対する変更提案の一覧](#8-仕様に対する変更提案の一覧)

---

## 1. 事前検証の結果

実装方針に影響する 4 点を、計画確定前に実機で確認した。

### 1.1 Vision: 新 API を採用する（仕様からの変更）

仕様 9 章は `VNRecognizeTextRequest` を指定しているが、**新しい `RecognizeTextRequest`（Swift 専用・構造体・async）を採用することを提案する。**

SDK で確認した事実:

| 項目 | `VNRecognizeTextRequest`（旧） | `RecognizeTextRequest`（新） |
|---|---|---|
| 形態 | `class`（ObjC 由来・可変参照） | `struct`（`Sendable`） |
| 実行 | `VNImageRequestHandler.perform` 同期 | `try await request.perform(on:)` |
| 言語指定 | `[String]` | `[Locale.Language]` |
| Swift 6 厳格並行 | 参照型のため隔離の扱いが煩雑 | **警告ゼロで通る** |

採用理由は Swift 6 の厳格並行モードとの相性である。本アプリは「OCR でメイン UI をブロックしない」（非機能要求・応答性）を満たすため OCR を並行実行するが、旧 API は参照型のため `Sendable` 境界を越える際に追加の隔離設計が必要になる。新 API は値型かつ async なので、`await` するだけで要求を満たせる。

**検証済み**: 下記コードが `-swift-version 6 -strict-concurrency=complete` で**警告ゼロでコンパイル**することを確認した。

```swift
var request = RecognizeTextRequest()
request.recognitionLevel = .accurate
request.recognitionLanguages = [Locale.Language(identifier: "ja-JP"),
                                Locale.Language(identifier: "en-US")]
request.usesLanguageCorrection = true
let observations = try await request.perform(on: cgImage)
```

なお `RecognizeTextRequest.Revision` は `revision3` のみで、これは旧 API の最新リビジョンと同一の認識エンジンである。**したがって仕様 6.1 の実測精度（日本語 99.5〜100%）はそのまま有効**であり、API 変更による精度の再検証は不要。認識レベル `.accurate`・言語指定・言語補正 on/off という 6.3 の設計もすべて新 API で表現できる（上記のとおり全プロパティが存在する）。

> 補足: 旧 API はヘッダ上で明示的に `deprecated` とはされていない（`API_AVAILABLE(macos(10.15))` のまま）。ただし Apple は macOS 15 以降 Swift 向けの新 API を推奨しており、新規実装で旧 API を選ぶ理由はない。

### 1.2 キャプチャ: フィルタ方式が必須（CAP-04 の要）

ScreenCaptureKit には範囲キャプチャの手段が 2 つあるが、**一方は CAP-04（オーバーレイが写り込まない）を満たせない。**

| API | 範囲指定 | 除外指定 | CAP-04 |
|---|---|---|---|
| `captureImage(in:)`（macOS 15.2+） | 引数 `rect` で直接 | **不可**（フィルタ引数がない） | **満たせない** |
| `captureImage(contentFilter:configuration:)`（macOS 14+） | `config.sourceRect` | `SCContentFilter(display:excludingWindows:)` | **満たせる** |

`captureImage(in:)` は範囲を引数で直接渡せて簡潔だが、コンテンツフィルタを受け取らないため**暗転オーバーレイ自身が写り込む**。範囲選択 UI が必ず画面を覆う本アプリでは採用不可である。

したがって以下の組み合わせを採用する（コンパイル検証済み）:

```swift
let myPID = ProcessInfo.processInfo.processIdentifier
let mine = content.windows.filter { $0.owningApplication?.processID == myPID }
let filter = SCContentFilter(display: display, excludingWindows: mine)

let config = SCStreamConfiguration()
config.sourceRect = rectInPoints            // 選択範囲（ポイント単位）
config.width  = Int(rectInPoints.width  * scale)   // scale = backingScaleFactor
config.height = Int(rectInPoints.height * scale)   // → Retina 解像度を維持（CAP-02）
config.captureResolution = .best
config.showsCursor = false
```

`sourceRect` はポイント単位で与え、`width`/`height` にその `backingScaleFactor` 倍を指定することで、ダウンスケールせず Retina 解像度のまま取得できる（CAP-02）。

### 1.3 権限: 署名 ID に紐づく（開発時の落とし穴）

検証中、未署名 CLI バイナリからのキャプチャは TCC エラーで失敗した。

```
Error Domain=com.apple.ScreenCaptureKit.SCStreamErrorDomain Code=-3801
"ユーザがアプリケーション、ウインドウ、ディスプレイ取り込みのTCCを拒否しました"
```

画面収録権限は**署名されたアプリバンドルの識別情報に紐づく**。実装上の帰結は 2 つある。

1. **権限は必ずアプリバンドルとして起動して確認する。** `swift` スクリプトや素のバイナリでは永久に取得できない。
2. **ad-hoc 署名だと署名が変わるたびに権限が失われる。** 開発中に「昨日は動いたのに今日は拒否される」が起きる典型原因。開発用の署名 ID を固定するのが望ましい。

また `CGPreflightScreenCaptureAccess()`（許可ダイアログを出さずに現状を確認）と `CGRequestScreenCaptureAccess()`（要求）が利用可能であることを確認した。受け入れ基準 10（権限が無いとき案内を出し、無反応にしない）は前者で分岐して実装する。

> 補足: ビルド時に `note: Disabling hardened runtime with ad-hoc codesigning.` が出る。ad-hoc 署名では Hardened Runtime が自動で無効化されるため、配布時は正式な署名 ID が必要。

**実際に発生した問題と恒久対策（段階 3）**: 当初 ad-hoc 署名（`Signature=adhoc` / `TeamIdentifier=not set`）でビルドしていたところ、「**許可を追加しても反映されず、再起動すると再び要求される**」状態になった。

原因は 2 つ重なっていた。

1. **ad-hoc 署名では TCC がビルドごとに別アプリと見なす。** 署名されたアプリは `Identifier` + `TeamIdentifier` で同一性が判定されるが、ad-hoc には Team ID が無いため `CDHash` が使われる。`CDHash` はビルドごとに変わるので、許可が毎回リセットされる。
2. **同じバンドル ID のコピーが 2 つ存在していた。** Xcode 既定の DerivedData 配下にも `JPScreenShot.app` が残っており、LaunchServices がそちらを起動していた。片方に許可を与えても、実際に動くのは別のコピーという状態だった。

対策として `project.yml` に開発用の署名 ID を固定した。

```yaml
CODE_SIGN_STYLE: Manual
CODE_SIGN_IDENTITY: "Apple Development"
DEVELOPMENT_TEAM: L897K2C26B
```

これで `TeamIdentifier=L897K2C26B` が入り、**リビルドしても同一性が保たれる**（`CDHash` が変わっても許可が維持される）ことを確認した。あわせて古い DerivedData のコピーを削除し、`tccutil reset ScreenCapture com.junpeiwada.JPScreenShot` で ad-hoc 時代の登録を消した。

> Bundle ID は当初 `com.soneru.JPScreenShot` だったが、JPPhotoTools（`com.junpeiwada.*`）と名前空間を揃えるため `com.junpeiwada.JPScreenShot` に変更した。TCC の許可は Bundle ID 単位のレコードなので、変更時は旧 ID の登録も `tccutil reset ScreenCapture com.soneru.JPScreenShot` で消しておく（残しても害はないが、使われない登録が溜まる）。

> 開発中に「許可したのに撮れない」が再発したら、まず `pgrep -lf JPScreenShot` で**実際に動いているバイナリのパス**を確認する。意図したビルドとは別のコピーが動いていることがある。

### 1.4 XcodeGen: project.yml をビルドまで検証済み

[3. XcodeGen 設定](#3-xcodegen-設定) の `project.yml` は、XcodeGen 2.44.1 で生成 → `xcodebuild` で **BUILD SUCCEEDED** まで確認した。生成された `Info.plist` に意図した値が入ることも検証した。

```
"CFBundleIdentifier" => "com.junpeiwada.JPScreenShot"
"LSMinimumSystemVersion" => "26.0"
"LSUIElement" => true          ← Dock に出さない（非機能要求・常駐）
```

`LSUIElement` は `INFOPLIST_KEY_LSUIElement: YES` で設定でき、**`Info.plist` を手書きで版管理する必要はない**（`GENERATE_INFOPLIST_FILE: YES` で生成される）。

## 2. ディレクトリ構成

仕様 9 章の想定構成を踏襲し、実ファイルまで具体化する。

```
JPScreenShot/
├── project.yml                 XcodeGen 定義（版管理する）
├── .gitignore                  *.xcodeproj / dd / .DS_Store を除外
├── README.md
├── Docs/
│   ├── 要求仕様.md
│   └── 実装計画.md              本ファイル
└── Sources/
    ├── App/
    │   ├── JPScreenShotApp.swift    エントリポイント（@main・NSApplicationDelegate）
    │   ├── AppCoordinator.swift     キャプチャ→OCR→結果表示の全体制御
    │   ├── MenuBarController.swift  NSStatusItem・左クリック即キャプチャ / 右クリックメニュー
    │   └── Settings.swift           環境設定（UserDefaults・認識モード記憶・保存先）
    ├── Capture/
    │   ├── ScreenCaptureService.swift   ScreenCaptureKit ラッパ（フィルタ方式）
    │   ├── OverlayWindow.swift          NSWindow サブクラス（全スクリーン・最前面）
    │   ├── OverlayView.swift            暗転・選択矩形・寸法表示・レティクル
    │   ├── SelectionCoordinator.swift   マルチディスプレイの一括制御・Esc 処理
    │   └── ScreenPermission.swift       CGPreflight/Request・システム設定への誘導
    ├── OCR/
    │   ├── TextRecognizer.swift     RecognizeTextRequest ラッパ（async）
    │   ├── RecognitionMode.swift    日本語 / コード の定義（言語・補正・後処理）
    │   └── CodeNormalizer.swift     全角→半角の正規化（コードモード）
    ├── UI/
    │   ├── ResultWindow.swift       NSWindow ホスト（SwiftUI を載せる）
    │   ├── ResultView.swift         上=画像 / 下=テキスト の縦積み（VSplitView）
    │   ├── ResultViewModel.swift    状態管理（OCR 進行中・再認識・編集）
    │   └── SettingsView.swift       環境設定画面
    └── Resources/
        └── Assets.xcassets          メニューバーアイコン（テンプレート画像）
```

分割の方針: 仕様 9 章の 4 分類（App / Capture / OCR / UI）を維持したうえで、**「権限」と「後処理」を独立ファイルにする**。どちらも仕様に明示された要求（受け入れ基準 8・10）で、単体で試験しやすくしておく価値が高い。

## 3. XcodeGen 設定

以下をそのまま `project.yml` として配置する（[1.4](#14-xcodegen-projectyml-をビルドまで検証済み) で検証済みの内容に、実装に必要な設定を追加したもの）。

```yaml
name: JPScreenShot
options:
  bundleIdPrefix: com.junpeiwada
  deploymentTarget:
    macOS: "26.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete     # 1.1 の新 API 採用が前提
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    ARCHS: "$(ARCHS_STANDARD)"             # Apple Silicon / Intel ユニバーサル
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: YES

targets:
  JPScreenShot:
    type: application
    platform: macOS
    sources:
      - path: Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.junpeiwada.JPScreenShot
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_LSUIElement: YES     # Dock に出さない
        COMBINE_HIDPI_IMAGES: YES
        ENABLE_APP_SANDBOX: NO             # 下記注記を参照
```

補足:

- **`.xcodeproj` は生成物なので `.gitignore` に入れる**（仕様 9 章）。`xcodegen generate` で再生成する。
- **Xcode の「Update to recommended settings」は `project.yml` 側で対応する。** Xcode のボタンで直すと生成物である `.xcodeproj` が書き換わり、次の `xcodegen generate` で消える。推奨設定の実体は Xcode 自身のテンプレート（`/Applications/Xcode.app/Contents/Developer/Library/Xcode/Templates/Project Templates/Base/Base_ProjectSettings.xctemplate/TemplateInfo.plist`）にあり、これと生成済みプロジェクトの差分を取れば不足分が特定できる。
  - 段階 6 時点で不足していたのは 5 つ: `SWIFT_APPROACHABLE_CONCURRENCY` / `SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY` / `ENABLE_USER_SCRIPT_SANDBOXING` / `LOCALIZATION_PREFERS_STRING_CATALOGS` / `ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS`。いずれも有効化して警告ゼロでビルドできることを確認済み。
  - `LastUpgradeCheck` は `options.xcodeVersion`（例: `"2650"`）で指定できる。
  - ただし **`compatibilityVersion` は XcodeGen 2.44.1 が `"Xcode 14.0"` に固定**しており、`project.yml` からは変更できない（`compatibilityVersion` / `projectVersion` / `objectVersion` はいずれもエラーにならず黙って無視される）。実害は確認されていないが、これが原因で警告が残る場合は XcodeGen 側の対応待ちになる。
- `SWIFT_VERSION` は Swift **言語モード**の 6 を指す（ツールチェーンの 6.3.2 とは別軸）。
- **App Sandbox は第 1 版では無効にする。** サンドボックス下では「デスクトップへの保存」（SAV-01）にユーザー選択やエンタイトルメントが必要になり、仕様の「デスクトップ固定でよい」という単純さと衝突する。App Store 配布に進む段階で、保存先選択（SAV-04）と併せて再検討するのが妥当。
- 外部依存はないため `packages` セクションは不要（仕様 3 章「外部依存: なし」）。

## 4. モジュール設計

### 4.1 全体の流れ

```
メニューバー左クリック
  → ScreenPermission.ensure()      権限が無ければ案内して中断（基準 10）
  → SelectionCoordinator.begin()   全スクリーンにオーバーレイ
       ├ Esc / 微小選択 / 対象なしクリック → キャンセル（CAP-05・4.2）
       ├ 矩形確定（ドラッグ）              → .region(CGRect)
       └ ウィンドウ確定（クリック）         → .window(SCWindow)   CAP-06
  → オーバーレイを閉じる            ※キャプチャ前に必ず閉じる（6 章参照）
  → ScreenCaptureService.capture()  .region: display + sourceRect（CAP-02/04）
                                    .window: desktopIndependentWindow（CAP-06/07）
  → 結果ウィンドウを表示            画像だけ先に出す（4.3・OCR を待たない）
  → TextRecognizer.recognize()      async・完了後にテキスト欄を更新
```

キャプチャ対象は `CaptureTarget`（`.region` / `.window`）で表す。`CGRect?` のままでは「矩形かウィンドウかキャンセルか」の 3 状態を表現できない。

要求「画像プレビューは OCR 完了を待たず先に表示する」（4.3）を満たすため、**結果ウィンドウの表示と OCR の開始を分離**する。ウィンドウは画像が取れた時点で出し、OCR 中はスピナーを表示、`await` の完了で差し替える。

### 4.2 認識モードと再認識

`RecognitionMode` は仕様 6.3 の表をそのまま型にする。

| モード | `recognitionLanguages` | `usesLanguageCorrection` | 後処理 |
|---|---|---|---|
| `.japanese`（既定） | `["ja-JP", "en-US"]` | `true` | なし |
| `.code` | `["en-US"]` | `false` | `CodeNormalizer` |

- モード切替で**即座に再認識**する（6.3）。キャプチャ画像は結果ウィンドウが保持しているので再キャプチャは不要。
- 直前のモードを `UserDefaults` に保存し次回の既定にする（6.3）。
- `CodeNormalizer` は仕様 6.3 の正規化対象（括弧類・記号・波ダッシュ／長音・全角空白）を実装する。**字形の混同（`l`/`1` 等）は補正しない**（6.3 の明示要求）。

**測定結果（段階 5・未検証項目の解決）**: 仕様 6.3 の注記「`["en-US"]` 単独指定で `l`/`1` の混同が実際に改善するかは未検証」を実測し、**大きく改善することを確認した**。

正解 `if (l == 1 && O != 0) { return; }` を Menlo 24pt で描画して測定:

| モード | 認識結果 | 一致率 |
|---|---|---|
| **コード**（`["en-US"]`・補正 off） | `if (l == 1 && 0 != 0) { return; }` | **97.0%** |
| 日本語（`["ja-JP","en-US"]`・補正 on） | `if （1 == 1 88 0 ！= 0） t returni ｝` | 69.7% |

**差は 27.3 ポイント**。コードモードでは以下がすべて正しく認識された。

- `l`（小文字 L）が `1` に化けない（日本語モードでは化けた）
- `&&` が `88` にならない
- `{` `}` `(` `)` `;` が半角のまま出る（全角化しない）

残る誤りは `O`（大文字オー）→ `0` のみで、これは仕様 6.3 の方針どおり**自動補正しない**（文脈がないと判断できず、正しい文字を壊す危険があるため）。

日本語の文章の精度も再確認した。

| ケース | 結果 |
|---|---|
| 日本語 28pt（2 行） | **100%**（完全一致） |
| 小さい文字 11pt（2 行・行間狭め） | **100%**（完全一致） |

仕様 6.1 の実測値（99.5〜100%）を新 API で再現できており、[1.1](#11-vision-新-api-を採用する仕様からの変更) の「認識エンジンは同一で精度は不変」という判断は妥当だった。

### 4.3 結果ウィンドウ

- レイアウトは**上に画像・下にテキストの縦積み**（4.3 の明示要求）。領域比をドラッグで変えられることが望ましいとあるため、SwiftUI の `VSplitView` を用いる。
- 画像は**アスペクト比を保ち、等倍を超えて拡大しない**（4.3）。`.aspectRatio(contentMode: .fit)` に加えて等倍を上限とする制約を入れる。
- テキストは**選択・編集可能**（4.3）。`TextEditor` を使い、コピー時は編集後の内容を用いる（CPY-02）。
- OCR が 0 文字なら「テキストを認識できませんでした」を表示し、**テキストコピーを無効化**（4.3）。
- コピー成功のフィードバックを出す（CPY-03）。コピー後に閉じるかは環境設定、既定は閉じる（CPY-04）。

## 5. 実装順序

各段階の終わりに動作を確認できる順序にする。前段が壊れていないか常に目視できる。

| # | 段階 | 内容 | 完了条件 |
|---|---|---|---|
| 1 | 土台 | `project.yml`・`.gitignore`・空の `@main`・メニューバー常駐 | Dock に出ず、メニューバーにアイコンが出る |
| 2 | 権限 | `ScreenPermission`・案内 UI | 未許可時に案内が出る（**基準 10**） |
| 3 | キャプチャ | オーバーレイ（暗転・レティクル・寸法・Esc）＋ フィルタ方式取得 | 範囲を撮れて、**オーバーレイが写らない**（基準 1/2/9・CAP-04） |
| 4 | 結果表示 | 結果ウィンドウ（画像のみ）・画像コピー・保存 | 基準 5/7 |
| 5 | OCR | `TextRecognizer`・非同期表示・テキストコピー | 基準 3/4/6 |
| 6 | モード | `RecognitionMode`・`CodeNormalizer`・切替で再認識・記憶 | 基準 8 |
| 7 | 仕上げ | 環境設定・About・非機能要求の計測 | 全基準・8 章の目標値 |

段階 3 を早い位置に置くのは、**CAP-04 と マルチディスプレイが最も失敗しやすい**ためである（[6 章](#6-技術的な要注意点)）。ここが通れば残りは素直に積める。

## 6. 技術的な要注意点

実装中に問題になりやすい箇所を、対処と併せて挙げる。

### 6.1 座標系の不一致（最大の注意点）

macOS には座標系が 3 つ混在する。**マルチディスプレイ（CAP-03）で最も壊れやすい箇所。**

| 座標系 | 原点 | 使う場所 |
|---|---|---|
| AppKit (`NSScreen`) | **左下** origin・Y 上向き | オーバーレイ・マウスイベント |
| CoreGraphics (`CGDisplay`) | **左上** origin・Y 下向き | `SCDisplay` の対応付け |
| ピクセル | — | `config.width/height`（`backingScaleFactor` 倍） |

対処: 変換を 1 か所（`ScreenGeometry`）に集約し、各所で場当たりに `-` しない。副画面が主画面の上や左にある配置（負座標）で必ず確認する。

**注意点**: CoreGraphics の原点は「**主画面の左上**」であり、全画面統合矩形の上端ではない。ここを取り違えると副画面で位置がずれる。

**検証済み（段階 3）**: 実機の 2 画面構成（2560×1440 が横並び、副画面 x=2560）で以下を確認した。

| 検証項目 | 結果 |
|---|---|
| 往復変換の一致（負座標を含む） | OK |
| 主画面上端 → CG で y=0 | OK |
| 副画面の選択 → ディスプレイローカル座標（x=300、2860 ではない） | OK |
| 境界をまたぐ選択 → 面積が大きい画面に寄る | OK |
| 全画面外の選択 → nil | OK |

### 6.2 オーバーレイの写り込み（CAP-04）

`excludingWindows` でアプリのウィンドウを除外するが、**それだけに頼らず、キャプチャ前にオーバーレイを閉じて確実にする**。閉じた直後の 1 フレームは画面がまだ暗いことがあるため、閉じてから撮る順序を守る（[4.1](#41-全体の流れ) の順序）。

### 6.3 オーバーレイウィンドウの設定

**実装時に踏んだクラッシュ（段階 3）**: `NSWindow` の指定イニシャライザは **`init(contentRect:styleMask:backing:defer:)` の 1 つだけ**である（`screen:` 付きは convenience。SDK の `NSWindow.h` で `NS_DESIGNATED_INITIALIZER` が付いているのは前者のみ）。

これを `override` せずに独自の `init(screen:)` を定義すると、継承した指定イニシャライザが未実装のまま残り、AppKit が内部的に呼んだ時点で実行時クラッシュする。

```
Fatal error: Use of unimplemented initializer
'init(contentRect:styleMask:backing:defer:)' for class 'OverlayWindow'
```

対処: 指定イニシャライザを `override` して初期化処理をそこに置き、`init(screen:)` は `convenience` にして `self.init(...)` 経由で通す。`screen:` 付きは convenience なので `override` できない（試すと *initializer does not override a designated initializer* でコンパイルエラー）。

- `level` を `.screenSaver` 相当にして最前面に出す。
- `NSWindow` は既定でキーウィンドウになりにくいため、`Esc` を拾うには `canBecomeKey` を上書きするか `NSEvent` のローカルモニタを使う。
- カーソルは `NSCursor.crosshair`。`cursorUpdate` / `resetCursorRects` で確実に適用する。
- 全スクリーン分のウィンドウを作り、**どれか 1 つで確定したら全部閉じる**。

### 6.4 起動の速さ（200ms 以内・非機能要求）

`SCShareableContent` の取得には時間がかかる。クリックしてから取得を始めると 200ms を超えるおそれがある。対処として、**オーバーレイの表示を先に行い、`SCShareableContent` の取得は並行して進める**（ユーザーが範囲をドラッグしている間に完了させる）。

**CAP-06 で生じた追加の考慮**: ウィンドウ選択は `SCShareableContent` を**選択中に**必要とする（ホバーのハイライトとクリックの確定に使う）。範囲選択のようにキャプチャ時まで待てないため、先読みの完了を `SelectionCoordinator.updateShareableContent(_:)` で注入する。

注入時には、次にマウスが動くのを待たずその場でハイライトを引き直す。さもないとオーバーレイが出た直後に素早くクリックした場合、`hoveredWindow` が nil のままキャンセル扱いになり「クリックしたのに何も起きない」状態になる。同じ理由で `begin` の末尾でも現在のカーソル位置でハイライトを初期化する（オーバーレイ表示直後はマウスが動いておらず `mouseMoved` が来ない）。

### 6.11 ウィンドウの特定（CAP-06）

オーバーレイが全画面を覆っているため、AppKit のヒットテストでは常に自分のオーバーレイが当たる。`SCShareableContent.windows` をクリック座標で自前に走査する（`WindowPicker`）。

判定で除外するもの:

| 除外対象 | 判定方法 | 理由 |
|---|---|---|
| 壁紙・Dock・メニューバー・通知センター | バンドル ID の列挙 | 全画面を覆う巨大な矩形として候補に混ざると他を選べなくなる |
| 上記から漏れたシステム UI | `windowLayer != 0` | 通常のアプリウィンドウは layer 0。ポップアップやステータス項目は正の値 |
| 自アプリのウィンドウ | `owningApplication.processID` | オーバーレイや結果ウィンドウを撮らない |
| 別 Space・最小化のウィンドウ | `isOnScreen == false` | 表示されていないものは選べない |
| 不可視の極小ウィンドウ | 辺が 8pt 未満 | 掴んでも「何も撮れていない」状態になる |
| **他のウィンドウに覆われたもの** | **可視面積の自前計算** | ユーザー要求「見えていない window は対象にしなくてよい」 |

#### 並び順は前面→背面（実測で確認）

`SCShareableContent.windows` の並び順を `CGWindowListCopyWindowInfo(.optionOnScreenOnly)`（前面→背面が保証されている）と比較したところ、**windowID の列が完全に一致した**。したがって「クリック点を含む最初のもの」を最前面として扱ってよい。

当初は「順序は明文化された保証ではないから面積最小を採る」という回避策を入れていたが、実測で確認できたため不要になった。面積最小は「ダイアログは親ウィンドウより小さい」という経験則に頼るヒューリスティクスで、大きいウィンドウが小さいウィンドウの前面にある場合に誤る。

#### `isOnScreen` は可視判定にならない（重要）

`isOnScreen` は「Space 上に配置されている」の意味であり、**他のウィンドウの背後に完全に隠れていても `true` を返す**。実測で、重なり合った Chrome 3 枚・VS Code 2 枚がすべて `isOnScreen == true` だった。`isActive` も同じ値を返し、可視判定には使えない。

そのため重なりを自前で計算する。前面→背面の順を利用し、対象より手前にあるウィンドウ群の矩形で覆われた面積を求め、**可視面積が 2% 以下なら候補から外す**。

面積の計算に単純な足し算は使えない（覆う矩形どうしが重なっていると同じ場所を二重に数え、可視面積を過小評価する）。矩形の数は数十程度なので走査線方式で正確に求める: 対象を X 座標の切れ目で縦の帯に分け、各帯ごとに Y 区間の合併長を出して足し合わせる。

閾値を 0%（完全一致）にしないのは、数ピクセルだけ端がのぞいているウィンドウを選べても実用にならないため。

#### ハイライトと実際に撮れるものの一致

`mouseUp` の時点でウィンドウを引き直すのではなく、ホバー時に確定した `hoveredWindow` を使う。引き直すと、その瞬間に前面のウィンドウが変わっていた場合に「見えているハイライトと撮れるもの」がずれる。

同一判定は `windowID` で行う。`SCWindow` のインスタンスは `content` を取り直すと別物になるため、参照比較では毎回ハイライトを再描画してしまう。

### 6.12 ウィンドウキャプチャと影（CAP-07）

`SCContentFilter(desktopIndependentWindow:)` を使う。対象ウィンドウだけを切り出すため、背後のウィンドウや壁紙は写らず、**手前に重なったウィンドウも写り込まない**。`display` + `sourceRect` 方式ではこれが防げない。

#### `ignoreShadowsSingleWindow` は使わない（ぼやけの原因）

影は `SCStreamConfiguration.ignoreShadowsSingleWindow = false` で得られる。しかし **これを使うと目に見えてぼやける**。

原因は次の組み合わせ:

1. 影を含めると実際の撮影範囲がウィンドウ枠より外側に広がる
2. しかし `SCContentFilter.contentRect` は**影を含まない範囲**を返す（実測で `SCWindow.frame` と完全に同一だった）
3. `config.width` / `height` に指定した値へ内容が合わせ込まれる（実測: 指定値がそのまま返る）

結果、影を含んだ広い内容が影なし相当の狭いピクセル数へ押し込められ、縮小される。

| 設定 | 鮮明度 |
|---|---|
| 影なし | **7.684**（整数 `sourceRect` の 7.629 と同水準） |
| 影あり（`ignoreShadowsSingleWindow = false`） | **4.625** |

4.625 は [6.9](#69-ぼやけの真因-sourcerect-の小数段階-6解決) でぼやけを踏んだときの 5.132 より更に悪い。

#### 対策: 影を自前で合成する（`ShadowCompositor`）

影を除外して等倍・鮮明に撮り、CoreGraphics で影を合成する。

```swift
config.ignoreShadowsSingleWindow = true   // 常に影なしで撮る
// …撮影後…
return ShadowCompositor.addShadow(to: image, scale: scale)
```

合成後にウィンドウ本体の領域を切り出して元画像と比較したところ、**最大差 0（ピクセル完全一致）**。鮮明度を一切犠牲にしていないことを確認済み。

実装上の要点:

- 影の Y オフセットは**負**にする（`CGContext` の Y は上向きなので、画面上で下に落とすには負）
- 余白はぼかし半径の 2 倍強を確保する。半径ぶんだけではガウシアンの裾が切れて影が直線的に途切れる。下方向はオフセットの分を足す
- 影の寸法はポイントで持ち、描画時に画像の倍率を掛ける。ピクセル固定だと Retina で影だけ相対的に細くなる

#### 影の形は画像自身のアルファから作る（角丸半径を推測しない）

**ScreenCaptureKit が返す画像には、ウィンドウの角丸がアルファとして正しく入っている。** 画像は矩形だが四隅は透明で、実測すると半径 15px の輪郭がアルファに現れた。

`CGContext.setShadow` は「これから描くもの」の形に影を落とすので、**画像をそのまま描けば角丸に沿った正確な影になる**。

```swift
context.saveGState()
context.setShadow(offset: CGSize(width: 0, height: -offsetY), blur: blur, color: shadowColor)
context.draw(image, in: imageRect)   // 画像のアルファ形状どおりの影ができる
context.restoreGState()
context.draw(image, in: imageRect)   // 影なしで上書き（下記）
```

**実装時に踏んだ誤り**: 当初「影を落とす形を不透明な角丸矩形で先に塗り、元画像も同じ形でクリップする」方式にした。角丸半径を 10pt とハードコードしたが、実際は 15px だったため、**差分の角が白い三角として四隅に残った**。角丸半径はアプリや macOS のバージョンで変わるため、推測してはいけない。画像自身から取れば常に正確になる。

#### 画像は 2 回描く

1 回目は影を出すため、2 回目は影なしで上書きするため。半透明なウィンドウでは、1 回目の描画で下に敷かれた影が透けて中身が暗く見える。同じ位置に影なしで重ねれば、見えるのは本来の色になる。

本体領域を元画像とピクセル比較して、**3 種のウィンドウすべてで最大差 0（無劣化）**を確認済み。

#### `config.width` / `height` は省略できない

省略すると既定サイズ（**1920×1080**）に強制される。実測で、2560×1331 のウィンドウが 1920×1080 に縮小されて返った。

### 6.5 Swift 6 厳格並行

`SCStreamConfiguration` / `SCContentFilter` / `SCShareableContent` は ObjC 由来の参照型で `Sendable` ではない。`@MainActor` に置くか、生成から使用までを同一の隔離域に閉じる。OCR 側は [1.1](#11-vision-新-api-を採用する仕様からの変更) の新 API により値型で扱えるので、境界を越えるのは `CGImage`（`Sendable` として扱える）に限定できる。

**実装時に判明した制約（段階 3）**: 非 `Sendable` 型は **`Task` の結果型にできない**。`Task<Success, Failure>` の `Success` は隔離に関係なく `Sendable` を要求するため、`Task { @MainActor in ... }` と書いても回避できない。

```swift
// NG: SCShareableContent は非 Sendable なのでコンパイルできない
private var preload: Task<SCShareableContent, Error>?

// OK: 結果は MainActor 隔離のプロパティに格納し、Task は Void を返す
private var preloadedContent: SCShareableContent?
private var preloadTask: Task<Void, Never>?
```

[6.4](#64-起動の速さ200ms-以内非機能要求) の先読みはこの形で実装した。`await preloadTask?.value` で完了を待ち、値はプロパティ経由で受け取ることで境界を越えさせない。

### 6.6 保存（SAV-02）

`JPScreenShot_YYYY-MM-DD_HHmmss.png` は秒単位なので、**同一秒に 2 回保存すると衝突する**。既存ファイルがあれば連番を付けて上書きを避ける（SAV-02 の「上書きしない」）。失敗時は必ず提示する（SAV-03・黙って失敗しない）。

### 6.8 「キャプチャがぼやける」の調査結果（段階 6）

保存した PNG がぼやけて見えるという指摘を受けて調査した。結論は **キャプチャ経路に欠陥はなく、ディスプレイが物理的に 1x であることが原因**である。

検証内容と結果:

| 検証 | 結果 |
|---|---|
| 3 方式で撮り比べ（`sourceRect`+size / 全画面から切り出し / `sourceRect` のみ） | 前 2 者は同一。3 番目は後述の罠あり |
| macOS 標準 `screencapture -R` と画素単位で比較 | **鮮明度 7.658 vs 7.920 でほぼ同等**。標準コマンドも同じくぼやける |
| `CGDisplayCopyAllDisplayModes` で HiDPI モードを探索 | **両ディスプレイともに HiDPI モードが 0 件**（20 モード中 0、17 モード中 0）。最大解像度も 2560×1440 |
| 2 倍要求して 1 倍へ縮小し情報量を比較 | 鮮明度 **1.789 vs 6.439**。2 倍要求では情報は増えず、補間で劣化するだけ |

つまり画面自体が 1 ピクセル = 1 ポイントで、**元の画面に含まれていない情報は取得できない**。macOS 標準のスクリーンショットと同じ見え方であり、これがこの環境での上限である。

**重要な罠**: `SCStreamConfiguration` の `width`/`height` を指定せずに `sourceRect` だけ設定すると、**既定値の 1920×1080 に引き伸ばされる**（実測: 500×160 の範囲を要求して 1920×1080 が返った）。必ず `width`/`height` を明示すること。現在の実装は指定済み。

**やってはいけない対策**: 「Retina 相当にするため 2 倍で要求する」のは逆効果である。上表のとおり情報は増えず、補間によってかえって甘くなる。仕様 6.1 が「Retina 解像度は必須ではない。1x と 2x の差はわずか」と記録しているのと整合する。

表示側では、等倍で収まる場合に `.resizable()` を経由しないようにした（同一寸法でもリサンプリング経路に入ると僅かに甘くなるため）。縮小が必要な場合のみ `.interpolation(.high)` を使う。

### 6.7 メモリ（非機能要求）

キャプチャ画像は結果ウィンドウを閉じたら解放する。ウィンドウを閉じても `NSWindow` が保持され続けて `CGImage` が残る事故が起きやすいので、閉じる際に明示的に参照を切る。

## 6.10 非機能要求の計測結果（段階 7）

仕様 8 章の目標値に対する実測。

| 項目 | 目標 | 実測 | 判定 |
|---|---|---|---|
| キャプチャ開始 | 200ms 以内 | オーバーレイ表示は即時。`SCShareableContent` 取得（平均 **0.097 秒**）は選択中に先読み | 達成 |
| キャプチャ実行 | — | `captureImage` 1000×600 で平均 **0.161 秒** | — |
| OCR | 1 秒以内 | **行数に依存**（下表） | **条件付き** |

### OCR の所要時間は行数に比例する

画像の面積ではなく**認識される行数**でほぼ決まる（ja+en・言語補正 on）。

| 行数 | 画像サイズ | 時間 |
|---|---|---|
| 2 行 | 1000×120 | 0.619 秒 |
| 4 行 | 1000×200 | 0.763 秒 |
| 8 行 | 1000×320 | 1.220 秒 |
| 12 行 | 1000×420 | 1.685 秒 |
| 18 行 | 1000×600 | 2.588 秒 |
| 25 行 | 1000×760 | 3.380 秒 |

**目標の 1 秒以内を満たすのは概ね 5 行程度まで**で、それを超えると超過する。仕様 8 章は「一般的な範囲（1000×600 程度）で 1 秒以内」としているが、1000×600 に 18 行入ると 2.6 秒かかる。**この目標は行数の想定が楽観的だった。**

### 言語補正のコストと、それでも有効にする理由

遅さの主因は言語補正である（18 行の場合）。

| 設定 | 時間 |
|---|---|
| ja+en・補正 **on** | 2.885 秒 |
| ja+en・補正 off | 1.259 秒 |
| ja のみ・補正 off | 1.345 秒 |
| en のみ・補正 off | 0.880 秒 |

補正を切れば 1 秒台に収まるが、**精度が落ちるので採用しない**。実際の日本語文章（800×200・4 行）で比較した結果:

| 設定 | 時間 | 一致率 |
|---|---|---|
| 補正 on | 0.860 秒 | **100%**（完全一致） |
| 補正 off | 0.492 秒 | 99.1%（`ピ` → `ビ` の誤り） |

このアプリの価値は「読める品質のテキストが得られること」（受け入れ基準 4）にあり、0.4 秒短縮するために精度を落とすのは本末転倒である。**仕様 OCR-02 が `.accurate` を要求している趣旨とも整合する。**

なお初回のモデルロードによる遅さではないことも確認した（同一設定で 5 回連続実行して 3.698 / 3.343 / 3.193 / 3.351 / 3.003 秒）。

### 体感への影響は小さい

OCR-01 / 4.3 の設計により、**画像プレビューは OCR を待たずに先に表示される**。OCR 中もスピナーが出るだけで、ウィンドウ操作・画像コピー・保存はブロックされない。行数が多い場合にテキストの表示が数秒遅れるが、待たされている間も他の操作はできる。

改善余地（第 1 版では実施しない）:

- `minimumTextHeightFraction` を上げて微小な文字を無視する（対象行数が減る）
- 行数が多い場合だけ補正を切る（精度が落ちる箇所が予測できないため保留）

## 7. 受け入れ基準との対応

仕様 10 章の 10 項目が、どこで満たされるかの対応表。

| 基準 | 内容 | 実装箇所 | 段階 |
|---|---|---|---|
| 1 | クリックでレティクル・ドラッグ選択 | `MenuBarController` / `OverlayView` | 3 |
| 2 | Esc でキャンセル | `SelectionCoordinator` | 3 |
| 3 | 画像とテキストが上下に表示 | `ResultView` | 5 |
| 4 | 日本語 OCR が目視で読める品質 | `TextRecognizer` | 5 |
| 5 | 画像コピーが他アプリに貼れる | `ResultViewModel` | 4 |
| 6 | テキストコピーが他アプリに貼れる | `ResultViewModel` | 5 |
| 7 | デスクトップに PNG 保存 | `ResultViewModel` | 4 |
| 8 | コードモードで全角→半角 | `CodeNormalizer` | 6 |
| 9 | マルチディスプレイで撮れる | `SelectionCoordinator` | 3 |
| 10 | 権限が無いとき案内が出る | `ScreenPermission` | 2 |

## 8. 仕様に対する変更点の一覧（確定済み）

本計画で仕様から変更した点。いずれも要求の意図は変えていない。**3 点すべてユーザー承認済み（2026-07-28）。**

| # | 箇所 | 仕様 | 本計画 | 理由 |
|---|---|---|---|---|
| 1 | 9 章 OCR | `VNRecognizeTextRequest` | `RecognizeTextRequest`（新 API） | Swift 6 厳格並行と適合。認識エンジンは同一で**実測精度は不変**（[1.1](#11-vision-新-api-を採用する仕様からの変更)） |
| 2 | 5.1 キャプチャ | ScreenCaptureKit（API 未指定） | フィルタ方式に限定（`captureImage(in:)` は不可） | `captureImage(in:)` では CAP-04 を満たせない（[1.2](#12-キャプチャ-フィルタ方式が必須cap-04-の要)） |
| 3 | — | 記載なし | App Sandbox を無効 | デスクトップ保存（SAV-01）の単純さを保つため（[3 章](#3-xcodegen-設定)） |

### 8.1 App Sandbox 無効の位置づけ

本アプリは**作者自身が使う個人ツール**という前提で第 1 版を作る。サンドボックス無効の帰結は次のとおり。

- **利点**: デスクトップへの保存（SAV-01）が追加の許可なしにそのまま動く。ユーザー選択ダイアログもエンタイトルメントの調整も不要で、「撮る→保存」が 1 アクションで完結する（仕様 1 章の目的に直結）。
- **制約**: **Mac App Store では配布できない**（App Store はサンドボックス必須）。直接配布や自分用ビルドは問題ない。
- **将来**: App Store 配布に進む場合はサンドボックスを有効化し、保存先選択（SAV-04）と併せて設計し直す。画面収録権限（[1.3](#13-権限-署名-id-に紐づく開発時の落とし穴)）は正式な署名 ID が前提になる。

### 6.9 ぼやけの真因: sourceRect の小数（段階 6・解決）

[6.8](#68-キャプチャがぼやけるの調査結果段階-6) では「1x ディスプレイだから上限」と結論したが、**これは誤りだった**。真因は `sourceRect` にサブピクセル座標が入っていたことである。

`SCStreamConfiguration.sourceRect` に小数が含まれると、ScreenCaptureKit がピクセル境界の中間から取得するため補間が入り、目に見えてぼやける。

実測（同じ領域・同じ静止画面）:

| `sourceRect` | 鮮明度 |
|---|---|
| 整数 `(60, 150, 724, 223)` | **7.629** |
| `(60.5, 150.5, …)` | 5.132 |
| `(60.37, 150.62, …)` | 5.132 |
| macOS 標準 `screencapture -R` | 7.629 |

**ドラッグの座標は `NSEvent.mouseLocation` 由来で小数を含むため、対策しないとほぼ毎回この劣化を踏む。** これが「保存してもぼやけている」の原因だった。

対策は 2 か所。

1. `SelectionCoordinator.rect(from:to:)` で選択矩形を整数に丸める（選択枠の表示と実際に撮れるピクセルを一致させる）
2. `ScreenCaptureService` で `sourceRect` を整数に整列させる（`origin` は `.down`、寸法は `.rounded()`）

修正後、小数を含むドラッグ座標でも **5.145 → 7.606** に回復し、macOS 標準（7.629）と同等になった。

**調査を誤らせた要因**（同じ轍を踏まないための記録）:

- 鮮明度を「別々のタイミングで撮った画像」同士で比べていた。画面内容が変わるため差が出ても意味がない。**静止画面で撮り直して画素を直接比較**すべきだった（それをやったら差 0.000% で一致した）
- テスト用に**整数座標**でキャプチャしていたため、実際のドラッグで起きる小数の問題を再現できていなかった。合成テストが通るのに実機で再現する場合、**入力値が実環境と同じか**を疑う
- 保存経路・色空間・表示の `.resizable()` を順に疑ったが、いずれも実測では無関係だった（保存経路は両方式で 7.629、色空間指定は改善せず、表示は 1.002 倍で等倍）
